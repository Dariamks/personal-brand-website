#!/usr/bin/env tsx
// OG image generation for blog posts using satori + resvg
// This is a placeholder — install satori and @resvg/resvg-js to enable.
// Run during build to generate /og/[slug].png for each blog post.

console.log("OG generation skipped (satori not installed).");
process.exit(0);
